specVersion: "v1"
serviceId: "mockwise"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mockwise-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
