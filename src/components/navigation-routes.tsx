interface NavigationRoutesProps{
    isMobile?: boolean
}

export const NavigationRoutes = ({isMobile = false,} : NavigationRoutesProps) => {
  return (
    <div>
      
    </div>
  )
}

